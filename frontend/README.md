# Frontend (Vite + React + Tailwind)

Install:
1. cd frontend
2. npm install
3. npm run dev

Notes:
- Tailwind needs PostCSS; this scaffold includes basic config files.

Added pages: Contacts, Deals, Tasks. Use the nav to reach them.
