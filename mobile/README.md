# Mobile (Expo)

Install:
1. cd mobile
2. npm install
3. npx expo start

Note: The mobile app uses the backend at http://localhost:4000 by default. On a physical device, replace with your machine IP.
